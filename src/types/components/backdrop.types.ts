/**
 * @file backdrop.types.ts
 * @description Tipos del overlay de carga global superpuesto a la aplicación.
 */

/** Props del componente Backdrop. */
export interface BackdropProps {
  isVisible: boolean;
}
